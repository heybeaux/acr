# engram-code

Semantic code search across indexed repositories via **engram-code** (localhost:3002).

**Key sections:** Skill: engram-code (Code Search), When to Use, Quick Search, Filter by language, Filter by chunk type (class, method, function, component, trigger, test), Change result count (default 5), Ensemble search (better accuracy, uses multiple models), Direct API Usage

**Provides:** engram-code
