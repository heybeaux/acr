name: consult
type: capability
version: 0.1.0
description: "Multi-model expert panel consultation. Use when: (1) stuck on a problem for 20+ minutes, (2) architecture or pricing decisions with irreversible consequences, (3) user says /consult, (4) genuine bl..."

provides:
  - consult
  # TODO: Add more specific provides tags

requires:
  tools: []
    # TODO: Declare required MCP tools
  capabilities: []
  context: []

budget:
  index: 24
  summary: 147
  standard: 992
  # deep: 1984  # Uncomment and create deep.md if needed

activation:
  triggers:
    - type: pattern
      match: "consult"
    - type: pattern
      match: "expert panel"
    - type: pattern
      match: "expert opinions"
    - type: pattern
      match: "multiple models"
    - type: pattern
      match: "multiple AI"
    - type: pattern
      match: "get perspectives"
    - type: pattern
      match: "second opinion"
  trigger_logic: OR
  co_activates: []
  conflicts: []

# permissions:
#   tools: {}
#   data: {}

behavioral:
  core: |
    # TODO: Extract the core behavioral instructions from standard.md
    # This should be the irreducible "how to do the thing" essence.
    See standard.md for full instructions.
  overlays: []

# state_schema:
#   version: 1
#   max_size_tokens: 200
#   fields: []

# verification:
#   checklist: []
#   completion_signal: task_complete
