# coding-agent

Use **bash** (with optional background mode) for all coding agent work. Simple and effective.

**Key sections:** Coding Agent (bash-first), ⚠️ PTY Mode Required!, ✅ Correct - with PTY, ❌ Wrong - no PTY, agent may break, Bash Tool Parameters, Process Tool Actions (for background sessions), Quick Start: One-Shot Tasks, Quick chat (Codex needs a git repo!)

**Provides:** coding-agent
